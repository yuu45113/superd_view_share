<div class="container">
  <div class="mt-3 mb-3">
    <span class="bg-light ml-5 px-1">
      <strong>会員一覧</strong>
    </span>
  </div>

  <div class="row">
    <table class= "table">
  　  <thead class="bg-light" >
       <tr><th>会員ID</th><th>ニックネーム</th><th>メールアドレス</th><th>ステータス</th></tr>
      </thead>
      <tbody>
        <% @users.each do |user| %>
          <tr>
            <td class="col-2">
              <%= user.id %>
            </td>

            <td class="col-2">
              <%= link_to admins_user_path(user.id) do %>
                <%= user.nickname %>
              <% end %>
            </td>

            <td class="col-4">
              <%= user.email %>
            </td>

            <td class="col-4">
              <% if user.is_deleted %>
                退会
              <% else %>
                <div class="text-success">
                  有効
                </div>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <div class="row justify-content-center">
    <div>
      <%= paginate @users %>
    </div>
  </div>
</div>