<div class="container">
  <div class="mt-3 mb-3">
    <div class="row">
      <div class="col-6 px-5 px-sm-0">
        <div class="text-center slider">
          <% @post.images.each do |image| %>
            <div>
              <%= image_tag image, size: "300x200", class: "d-inline-block" %>
            </div>
          <% end %>
        </div>

        <div class="d-flex">
          <div class="mt-2" id="favorite_button_<%= @post.id %>">
            ♥ <%= @post.favorites.count %>
          </div>
          <div class="mt-2">
            💬
          </div>
        </div>

      </div>
    </div>
  </div>
</div>
